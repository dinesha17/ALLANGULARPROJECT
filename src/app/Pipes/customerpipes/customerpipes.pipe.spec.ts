import { CustomerpipesPipe } from './customerpipes.pipe';

describe('CustomerpipesPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerpipesPipe();
    expect(pipe).toBeTruthy();
  });
});
